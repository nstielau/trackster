<h2>Welcome</h2>

<% if !current_user.nil? %>
  You have <%=current_user.tracks.size %> tracks.
  <ul>
  <% current_user.tracks.each do |t| %>
    <li><%= h t.inspect%></li>
  <% end %>
  </ul>
<% end %>
